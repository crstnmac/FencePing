global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  # - "first_rules.yml"
  # - "second_rules.yml"

scrape_configs:
  # The job name is added as a label `job=<job_name>` to any timeseries scraped from this config.
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Note: The following services don't currently expose Prometheus metrics
  # They are commented out to prevent 404 errors in logs
  # Uncomment and configure proper metrics endpoints when metrics are implemented
  
  # - job_name: 'geofence-api'
  #   static_configs:
  #     - targets: ['api:3001']
  #   metrics_path: '/metrics'
  #   scrape_interval: 30s

  # - job_name: 'geofence-dashboard'
  #   static_configs:
  #     - targets: ['dashboard:3002']
  #   metrics_path: '/metrics'
  #   scrape_interval: 30s